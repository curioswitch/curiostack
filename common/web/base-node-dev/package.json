{
  "name": "@curiostack/base-node-dev",
  "version": "0.0.1",
  "description": "Curiostack's base dev dependencies for non-browser applications.",
  "main": "build/index.js",
  "repository": "curioswitch/curiostack",
  "author": "Choko <choko@curioswitch.org>",
  "license": "MIT",
  "scripts": {
    "prebuild": "yarn run build:clean",
    "build": "babel --extensions '.ts' ./src -d ./build",
    "build:clean": "rimraf ./build",
    "//": "Normally we would lint before build, but we are actually building the lint config here.",
    "postbuild": "yarn run lint",
    "prepublishOnly": "yarn run build",
    "lint": "yarn run type-check && yarn run tslint --project tsconfig.json",
    "type-check": "tsc"
  },
  "dependencies": {
    "@babel/cli": "7.0.0-beta.36",
    "@babel/core": "7.0.0-beta.36",
    "@babel/plugin-transform-modules-commonjs": "7.0.0-beta.36",
    "@babel/preset-env": "7.0.0-beta.36",
    "@babel/preset-typescript": "7.0.0-beta.36",
    "prettier": "1.9.2",
    "rimraf": "2.6.2",
    "tslint": "5.8.0",
    "tslint-config-airbnb-base": "0.1.0",
    "tslint-config-prettier": "1.6.0",
    "tslint-plugin-prettier": "1.3.0",
    "typescript": "2.6.2"
  },
  "devDependencies": {
    "//": "tslint cannot read the config without module.exports",
    "babel-plugin-add-module-exports": "0.2.1"
  }
}
