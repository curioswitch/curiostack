/**
 *
 * {{ properCase name }}
 *
 */

import React from 'react';
{{#if wantMessages}}
import { FormattedMessage } from 'react-intl';
{{/if}}
// import styled from 'styled-components';

{{#if wantMessages}}
import messages from './messages';
{{/if}}

interface Props {
}

export default class {{ properCase name }} extends {{{ type }}}<Props> {
  public render() {
    return (
      <div>
      {{#if wantMessages}}
        <FormattedMessage {...messages.header} />
      {{/if}}
      </div>
    );
  }
}
