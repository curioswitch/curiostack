
import {
  defaultAction,
} from '../actions';

describe('{{ properCase name }} actions', () => {
  describe('Default Action', () => {
    it('has a type of app/{{ properCase name }}/DEFAULT_ACTION', () => {
      const expected = {
        type: 'app/{{ properCase name }}/DEFAULT_ACTION',
      };
      expect(defaultAction()).toEqual(expected);
    });
  });
});
